# Runtime dependencies
torch>=2.0.0
transformers>=4.30.0

# Data & metrics
pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.3.0

# Progress bars
tqdm>=4.65.0

# Plotting (training curves, confusion matrix, report heatmap)
matplotlib>=3.6.0
seaborn>=0.12.0

# Notes
# - Tkinter ships with CPython on Windows/macOS.
#   On Debian/Ubuntu install it with:  sudo apt-get install python3-tk
# - For GPU training install a CUDA-matched torch build first, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
